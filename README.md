# Langchain Tutor

A Streamlit-based chatbot application that uses the Model Context Protocol (MCP) to interact with LLMs (Google Gemini and Groq Llama).

## Run with Docker

Execute the following command to build and start the application:

```bash
docker-compose up --build
```

The application will be available at http://localhost:8501.
